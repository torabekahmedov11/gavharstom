{
  "name": "gavhar-stoma-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd admin-panel && npm install && cd ../client-web && npm install && cd ../backend && npm install",
    "build": "node build.js"
  }
}
